class Solution {
    public int findMin(int[] nums) {
        Arrays.sort(nums);
        // int left=0;
        // int right=nums.length;
        // while(left<=right){
        //     int mid=left+(right-low)/2;
        //     if(arr[mid])
        // }
        int a=nums[0];
        return a;
        
    }
}